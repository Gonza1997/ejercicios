#include <stdio.h>
#include <stdlib.h>

int main()
{
   char letra;

   int a=0, e=0, i=0, o=0, u=0, cons=0;

   printf("Ingrese la letra: ");
   letra = getche();

   if(letra != "a" ||letra != "e" ||letra != "i" || letra != "o" ||letra != "u")
   {
    switch(letra)
case "a":
        a++;
        break;
    case "e":
        b++;
        break;
    case "i":
        i++;
        break;
    case "o":
        o++;

   }
   else
   {

   }

    return 0;
}
